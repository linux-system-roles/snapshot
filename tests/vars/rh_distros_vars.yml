# vars for handling conditionals for RedHat and clones
# DO NOT EDIT - file is auto-generated
# repo is https://github.com/linux-system-roles/.github
# file is playbooks/templates/tests/vars/rh_distros_vars.yml
---
# Ansible distribution identifiers that the role treats like RHEL
__snapshot_rh_distros:
  - AlmaLinux
  - CentOS
  - RedHat
  - Rocky

# Same as above but includes Fedora
__snapshot_rh_distros_fedora: "{{ __snapshot_rh_distros + ['Fedora'] }}"

# Use this in conditionals to check if distro is Red Hat or clone
__snapshot_is_rh_distro: "{{ ansible_facts['distribution'] in __snapshot_rh_distros }}"

# Use this in conditionals to check if distro is Red Hat or clone, or Fedora
__snapshot_is_rh_distro_fedora: "{{ ansible_facts['distribution'] in __snapshot_rh_distros_fedora }}"

# Use these in conditionals to check if distro is Red Hat or clone of a specific major version
__snapshot_is_rh_distro_7: "{{ __snapshot_is_rh_distro and ansible_facts['distribution_major_version'] == '7' }}"
__snapshot_is_rh_distro_8: "{{ __snapshot_is_rh_distro and ansible_facts['distribution_major_version'] == '8' }}"
__snapshot_is_rh_distro_9: "{{ __snapshot_is_rh_distro and ansible_facts['distribution_major_version'] == '9' }}"
__snapshot_is_rh_distro_10: "{{ __snapshot_is_rh_distro and ansible_facts['distribution_major_version'] == '10' }}"
